Нужно довести до рабочего состояния букмарклет для madtest.ru. Пиши код по JAVASCRIPT.md: простые последовательные трансформации, маленькие методы, side effects отдельно, без лишних абстракций, без комментариев внутри логики.

Контекст:
1. Madtest — SPA. Один JS-бандл, но DOM после отрисовки разный на страницах /main, /questions, /results, /preview.
2. В админке тест имеет внутренний id в URL: /app/tests/186639/main.
3. Публичный код/slug на preview другой: например testId "f7Jd8aAU" и ссылка madte.st/f7Jd8aAU.
4. На /app видны карточки тестов со ссылками вида /app/tests/view/186639/stat. По ним можно собрать внутренние id.
5. Нужно не делать большой экспорт всего подряд. Нужны сценарии по выбору для текущей страницы.

Текущая архитектура:
- один объект madtest внутри IIFE;
- madtest.run(mode);
- режимы:
  - "auto": логин/роутинг по URL;
  - "export": экспорт текущей открытой страницы;
  - "sanitize": нормализация редактируемых полей текущей открытой страницы;
  - "find": поиск внутренней ссылки редактирования по публичному id/slugu madte.st;
- страницы регистрируются в madtest.pages:
  - main
  - questions
  - results
  - preview

Что нужно реализовать/проверить:
1. Логин:
   - если URL /app/login?r=/app/tests/192098/questions, заполнить логин/пароль, отправить форму, после логина перейти на r;
   - если r нет, перейти на /app/.
2. Jivo/консультант:
   - скрывать/удалять автоматически через style + MutationObserver.
3. Экспорт текущей страницы:
   - для main/questions/results собирать все доступные видимые редактируемые поля: input text/url, textarea, contenteditable;
   - формат текстового файла: поле, метка/name, значение;
   - для preview собрать ссылку madte.st и embed-код, если есть.
4. Нормализация текущей страницы:
   - брать только видимые редактируемые поля;
   - менять только если значение реально изменилось;
   - после изменения эмитить input/change;
   - правила:
     - NBSP -> обычный пробел;
     - схлопнуть повторные пробелы/табы;
     - ё -> е, Ё -> Е;
     - три точки -> многоточие;
     - дефисы/тире с пробелами -> длинное тире с пробелами;
     - прямые двойные кавычки -> «елочки» попарно;
     - прямой апостроф -> ’;
     - убрать пробелы перед/после переносов;
     - trim.
5. Поиск по публичному id:
   - запускать на /app;
   - собрать карточки тестов из ссылок /app/tests/view/{id}/stat;
   - для каждого id проверить /app/tests/{id}/preview через fetch(credentials: "include");
   - если HTML содержит публичный id, вывести ссылку https://madtest.ru/app/tests/{id}/main;
   - если fetch preview возвращает пустой SPA без содержимого, заменить стратегию на последовательный переход/открытие preview в текущей вкладке с sessionStorage-состоянием.
6. Важно: в questions/results может быть список элементов и выбранный элемент справа. Если видимые поля текущей страницы не покрывают все элементы списка, нужно добавить отдельный сценарий list.run(kind, action), который кликает каждый пункт списка, ждёт отрисовку формы и применяет export/sanitize к видимым полям выбранного элемента. Делать это аккуратно и только если DOM подтверждает необходимость.

Файлы, которые я тебе дам:
- JAVASCRIPT.md — обязательный стиль кода.
- madtest.bookmarklet.js — текущий черновик.
- madtest-app_outerHTML.txt — HTML страницы /app со списком тестов.
- 186639-main.txt и/или 186639-main (5).txt — HTML/экспорт main.
- 186639-questions (6).txt — экспорт questions.
- 186639-results (1).txt — экспорт results.
- 186639-preview (2).txt — preview/embed.
- по возможности HTML outerHTML страниц /main, /questions, /results, /preview после отрисовки.

Ограничения:
- не переписывай всё в сложный фреймворк;
- не добавляй сбор всех тестов целиком;
- не делай автосохранение, если явно не попросят;
- не трогай невидимые/служебные поля;
- не добавляй новые глобальные переменные, кроме window.madtest и опционально window.__madtestMode;
- код должен повторно запускаться без дублей наблюдателей и без поломки страницы.

Результат:
- дай финальный madtest.bookmarklet.js;
- отдельно дай короткие bookmarklet-запуски для режимов export/sanitize/find;
- укажи, какие селекторы были подтверждены HTML-файлами, а какие требуют проверки в браузере.
